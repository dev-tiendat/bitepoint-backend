export enum RedisKeys {
    AUTH_TOKEN_PREFIX = 'auth:token:',
    AUTH_PASSWORD_VERSION_PREFIX = 'auth:passwordVersion:',
    AUTH_PERMISSION_KEY_PREFIX = 'auth:permission:',
    TOKEN_BLACKLIST_PREFIX = 'token:blacklist:',
    TOKEN_PERSISTENT_PREFIX = 'token:persistent:',
}

export const API_CACHE_PREFIX = 'api-cache:';